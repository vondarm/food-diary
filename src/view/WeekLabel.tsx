import dayjs from "dayjs";import {type ReactNode, useMemo} from "react";import {useAverageKcalForWeek} from "./CoreAdapter.tsx";import { Alert } from "@mui/material";import type { AlertProps } from "@mui/material/Alert";import {  getDayOvereatingLevel,  type OvereatingLevel,} from "../core/meals/overeatingLevel.ts";import ReportProblemIcon from "@mui/icons-material/ReportProblem";import RestaurantMenuIcon from "@mui/icons-material/RestaurantMenu";import CheckCircleIcon from "@mui/icons-material/CheckCircle";export const WeekLabel = () => {  const today = useMemo(() => dayjs().startOf("day"), [])  const averageKcalForWeek = Math.round(useAverageKcalForWeek(today));  const overeatingLevel = getDayOvereatingLevel(averageKcalForWeek, 2500);  return (    <Alert      style={{ alignItems: "center" }}      icon={getIconByOvereatingLevel(overeatingLevel)}      severity={getDayColorByOvereatingLevel(overeatingLevel)}    >      {getWeekDescriptionByOvereatingLevel(overeatingLevel, averageKcalForWeek)}    </Alert>  );};const getDayColorByOvereatingLevel = (  level: OvereatingLevel,): AlertProps["severity"] => {  switch (level) {    case "low":      return "info";    case "normal":      return "success";    case "high":      return "warning";    case "extra high":      return "error";    case "incredible":      return "error";  }};const getIconByOvereatingLevel = (level: OvereatingLevel): ReactNode => {  switch (level) {    case "low":      return <RestaurantMenuIcon />;    case "normal":      return <CheckCircleIcon />;    case "high":      return <ReportProblemIcon />;    case "extra high":      return "🐷";    case "incredible":      return "🐷";  }};const getWeekDescriptionByOvereatingLevel = (  level: OvereatingLevel,  kcal: number,): string => {  switch (level) {    case "low":      return `В среднем ${kcal} ккал в день. На этой неделе мы недоедаем, можно поднажать`;    case "normal":      return `В среднем ${kcal} ккал в день. Неделя идет хорошо, близко к норме`;    case "high":      return `В среднем ${kcal} ккал в день. На этой неделе мы немного переедаем, надо ужаться`;    case "extra high":      return `В среднем ${kcal} ккал в день. На этой неделе мы сильно переедаем, пора остановиться`;    case "incredible":      return `В среднем ${kcal} ккал в день. Эту неделю придется исправлять долго`;  }};